# Expanded autonomous research

The user explicitly requested continued independent research toward a measured 0 score and delegated design choices. This is a new exploratory phase, not a retroactive success claim for the earlier protocol.

First batch: (1) continue recorded HIP4 to the paper's fixed N=10 with seeds 46–51, retaining every step and initially scoring only HIP10; (2) three source-grounded Qwen candidates using content-plan reconstruction, familiar lexical phrasing and rhetorical reordering. These change writing method, not punctuation tricks or invisible characters. Every candidate and error is retained. Only supplied general prose and synthetic cases are submitted to public checkers. No automated public-site score loop.

An observed 0 needs a second scan of the exact text and a separate check after any cleanup. Candidate quality is evaluated independently for factual and semantic loss. If a candidate has critical meaning errors, its low score is reported but it is not a faithful solution. A promising recipe is frozen and run on the two prepared unseen cases; all original/output measurements and failures are retained. No invented heuristic or LLM-critic percentage is presented as a real detector score. Broader methods may be explored in explicitly documented later batches under the user's renewed instruction.

HIP source: https://arxiv.org/html/2605.19516v1 (N=10, temperature1,top-p.95; actual paper evaluations GPTZero/Pangram, not our ZeroGPT). Production settings remain unchanged during experiments. UI redesign runs independently.

Additional control: the root agent authored a structurally different explanatory passage and submitted it once. It scored 0, but review caught unsupported specificity (hours) and a loose rendering of activity categories. Save a corrected control, scan it separately, and if still0 rescan its exact text. This is explicitly agent-authored, not app output or human ground truth. It tests whether faithful prose is feasible; a product method must reproduce it on its own inputs. Next probe uses original unrelated few-shot examples to teach this structural pattern to the connected models, never inserts the scored control as the requested final answer.

The corrected agent control scored0 twice and passed blind important-meaning review. This establishes only a feasible example, not an app method. HIP10 scored15.3 but failed meaning with an invented past success claim; not integrated. Few-shot Qwen scored94.4. A separate two-stage claim extraction plus composition was run in Qwen and Gemma (3 calls total); extraction itself strengthened a conditional into necessity and one writer invented extra conclusions. Keep these failures visible. AuthorMist download begins with an allowlist excluding its uploaded .venv; only research usage is planned under the upstream Qwen terms.

Cross-model batch: freeze three already generated drafts (demo-qwen, demo-gemma, claims-gemma) and apply exactly one HIP pass to each, seed42, then score the complete outputs and review fidelity. This tests whether a different starting voice gives the base paraphraser a better context without increasing sequential HIP drift. No re-sampling by score.

Lexical/structure ablation: two connected-model calls replace a prespecified set of institutional terms with equivalent everyday meanings and turn the four source risks into questions. This follows the measured agent control but does not insert its text as the answer or demonstration. No invisible characters or intentional typos. Every source claim must still survive independent review.

New observed result: lexical-Gemma scored0 on the public UI (actual configured-model output), lexical-Qwen42, cross-demo-Gemma16.7. Repeat lexical-Gemma unchanged to establish repeatability of that exact sample. It is not yet a faithful winner: initial review flags the invented removal of manual work and omitted original scope/activities. Get a blind review, then make one minimal source-grounded repair in a second model using everyday language and rescan the actual repaired text. Any0 claim must say which exact version achieved it.

Generalization batch: replace the screenshot-specific lexical instructions with a fixed general plain-English recipe (scripts/probe-general-plain.mjs) and run the original plus previously prepared library/survey texts once each on Gemma, same settings. These are exploratory unseen-case trials of this newly frozen recipe, not retroactive validation of the screenshot-specific prompt. Keep all outputs and actual scores; assess exact numeric/date/qualifier preservation.

The generic plain recipe scored32.6 on the overview,24.3 on library and57.5 on survey. Fresh source scores were100,69.8 and47.9 respectively: survey worsened, so do not claim general improvement or promote it as a proven default. All three passed blinded important-meaning review with minor issues. Qwen repair of lexical-Gemma scored25.6 and still failed omissions. One further explicitly editor-guided Gemma repair receives all issues from the blind review, retains its plain voice, and is measured separately. This assisted repair is not an autonomous quality guarantee.

Guided repair passed important-meaning review and scored14.3. The saved public screenshot visibly highlights the two efficiency/employee-benefit sentences. A single targeted Gemma rewrite changes ONLY that exact selected span, restores the omitted historical manual-effort comparison, and leaves the rest untouched. Record and score it as a new adaptive candidate, not an independent benchmark winner.

AuthorMist first generation returned256 tokens (below512) but the probe marked incomplete because it checked only tokenizer eos151645. The pinned generation config explicitly allows both151645 and151643. Correct the research completion check to honor the model's EOS list and save last token, top-k20 and repetition penalty1.05 (inherited defaults). Repeat the same seed once and retain the first record; this is a technical instrumentation correction, not a hidden candidate resample.

Continuation controls: three fixed source-grounded short prefixes are supplied in HIP's target field (time, risks, work), seed42, temperature1/top-p.95. A fourth control disables the existing adapter and provides two original generated unrelated paraphrase demonstrations to the base model. No extra model weights. This tests the primary paper's context hypothesis as rewriting, not merely unconstrained continuation. Prefix text is retained as part of the final candidate. All outputs saved, including malformed or truncated generations.

The targeted edit worsened14.3 to28.2; keep the earlier candidate as better measured, not latest-by-default. AuthorMist scored65.8 and repeated a summary; corrected EOS rerun produced exactly the same text. All four prefix/base controls have unsupported additions, losses or severe truncation and are not detector-scanned. One Qwen3.5 reasoning-enabled repair probe uses24576 max tokens to test whether the earlier8192 reasoning exhaustion masked better editing ability; one call only,180s timeout, explicit preserved failures. This changes reasoning configuration, not the first-stage draft or review list.

## Actual application validation
The integrated Plainspoken tone is evaluated through createApp, the actual protected-text pipeline and existing built-in guides, not the short standalone prompt. Three Gemma calls: overview and survey accepted; library rejected because the name guard includes the leading grammatical article. Retain rejected preview and fix narrowly. Check the two accepted results once each on ZeroGPT. Then run a distinct three-call DeepSeek Flash arm through the app, Strong, Plainspoken, no guides, temperature 0.9, top-p 0.95, 16384-token ceiling, no automatic second pass. This is another adaptive exploratory arm, not a preregistered holdout success claim.

DeepSeek arm: all three endpoint requests immediately rejected with HTTP 403; zero successful generations. No retries. Next isolate recipe/context effects using Gemma with the same Plainspoken prompt, Strong and zero optional guides on the same three exploratory texts. This is three additional calls, with raw output retained before any punctuation edit.

## Adaptive source-specific brief
The actual app overview scored 100 despite standalone recipe 32.6; app survey 62.7. Strong/no-guides still copied source organization. Test a different procedure: Gemma first extracts replaceable abstract phrases plus a source-claim inventory and new opening/order; a second Gemma call rewrites with this brief and the authoritative original source. Three same exploratory cases, at most six calls, temperature 0.9/top-p 0.95. This is structural planning before raw writing, not punctuation correction or a detector-driven retry loop. Retain all attempts.

## Integrated lexical recipe revision
The earlier app recipe yielded 100 on overview both Balanced/guides and Strong/no-guides, versus 28.3 from the planned brief. Preserve the old recipe in plain-app-recipe-v1.txt. Revise Plainspoken to the lexical recipe family that produced 0 in standalone research, removing source-specific instructions about four risks or starting with time. It retains explicit no-loss examples (long-term planning, faster versus no manual work), and all existing app protection. Evaluate three fresh application generations with this same frozen recipe, Gemma Strong/no-guides, no review. This is adaptive tuning on reused examples, not final held-out validation.

## Frozen recipe final exploratory validation
Integrated lexical recipe measured22.4/32.3/39.4 versus source100/69.8/47.9 on the reused overview/library/survey. Freeze src/prompt.mjs in heldout-final/prompt-snapshot.mjs and generate exactly once on two newly authored fictional garden-club and transit-survey texts; no previous use in tuning. Two generation calls, four public scans of full original/output, separate meaning review, no punctuation correction or resampling. A two-case check is small evidence, never a guarantee.
