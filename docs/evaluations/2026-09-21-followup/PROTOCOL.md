# Renewed bounded experiment

The user asked to continue research and achieve lower measured AI scores after the HIP precheck failed. This is a new experiment, not a retroactive relaxation of that precheck.

Hypotheses: (A) minimally repairing HIP's semantic drift may retain its measured score improvement; (B) a second HIP pass followed by the same repair may improve it further; (C) a released fidelity-trained Gemma rewriter may improve the quality/detector trade-off without iterative paraphrasing. Generic chat prompt variants already failed in earlier work and are not the main search direction.

Development input: existing screenshot AI overview. Freeze three candidates before external measurement: HIP1 + repair, HIP2 + repair, Gemma Q6_K first sample. Connected repair uses the existing Qwen model, one call, temperature 0.2, at most 4096 tokens, source authoritative, phrase-level corrections only. No score supplied to the editor. Gemma uses its exact published prompt wrapper, temperature 0.85, top-p 0.95, seed 42, maximum 900 new tokens, no chat template. No anti-copy guard in this first probe; record this deviation from the published evaluation.

Three development candidates at most; one actual public detector scan each. Keep every raw candidate and failed result. If a candidate has important meaning errors it cannot win even with a low score. Development success requires a displayed AI score below 50% and no important meaning errors. If only a lesser reduction is observed, report it as such. No unlimited retry or cosmetic typo/Unicode tricks.

If development succeeds, freeze the winning pipeline and evaluate two newly authored 150–230-word synthetic documents (library pilot and workplace survey) before selecting defaults. Check source and output once each on the same available public detector. Require at least a 20 percentage-point reduction on both and no important semantic errors. This small test supports only a limited demonstration, not a general detector success rate. A broader 12-document/two-detector claim is not possible without those actual scans.

Preserve production connection, skills and settings throughout isolated evaluation. Integrate a successful pipeline as an explicit option; retain failures visibly in the evidence. No live paid detector API call without existing credentials, no purchase, no hidden scraping endpoints or CAPTCHA bypass.

New primary sources: https://huggingface.co/jialinyyzz/humanizer-gemma-4-e4b (revision 93d4eee64b92e8b4ef331540fe1aadac0724dc00); https://github.com/sgaofen/humanizer (65799b5c89e2c4096423eb9267cf7a8061ae9796). Code Apache-2.0, weights subject to Gemma terms. HumanFlow's unsupported internal 99% claim was not treated as independent proof. Untell's bounded measurement approach was inspected; its runtime code was not imported.

Before any new checker scan: the first repair call returned a non-stop finish reason at 4096 tokens; the initial probe failed to retain the exact reason. Allow one technical retry at the existing production limit of 8192 tokens and save all subsequent finish reasons/usage, including failures. The precomputed HIP2 raw draft is reused. This is explicitly an additional failed model call, not a new candidate.

Transport diagnosis before any checker measurement: both retained repair responses spent all 8192 completion tokens in `reasoning_tokens`, with empty visible content. A final isolated compatibility probe uses the official Qwen `chat_template_kwargs.enable_thinking=false` switch, same source/prompt/model and token limit. This tests an evidenced transport/configuration cause; prior failures remain in `*-thinking-failed.json`. If ignored or rejected, do not keep retrying. Reference: https://huggingface.co/Qwen/Qwen3.5-122B-A10B#instruct-or-non-thinking-mode .

Observed development result so far: HIP1 + repair scored 52.4% on the public ZeroGPT UI and passed the important-meaning review with minor omissions and grammar issues. It misses the predeclared <50% threshold; do not silently promote it as a successful development winner. HIP2 + repair failed meaning fidelity and is not sent for a score. The successful direct-response API produced a top-level edit array instead of the requested object; accepted the structurally equivalent array and reapplied the saved response without another call.

User steering during download: prioritize lowering the raw detector score; defer punctuation/grammar cleanup to a second model. Minor punctuation errors do not reject a first-stage candidate. For the final selected pipeline, use one narrowly scoped cleanup pass and measure that final output again; do not claim the raw score for an edited text. Important meaning changes remain a separate reported failure. This supersedes treating surface grammar as a development rejection.

After user priority change: Gemma raw measured 100% and failed the semantic review (oversight→involvement and reliability→accuracy). It is not integrated. Measure the already-generated HIP2 raw draft once to distinguish a failed first-stage detector improvement from a failed cleanup; raw semantic defects remain explicit. Also allow one final grammar-only cleanup of the best measured HIP1 repaired draft, then rescan that exact final text. These are declared extensions after initial results, not a preregistered benchmark win.

Second-stage finding: HIP1 repaired draft scored 52.4%; grammar-only edits (two hyphens and a capitalization change) produced a separately scanned 100% result. This is an observed pair, not a proof of stable causal sensitivity. HIP2 raw scored 49% but has semantic drift. New bounded hypothesis under the user's raw-first instruction: test the paper's iterative mechanism to exactly four passes, reusing the already-recorded first two, with fixed seeds 44 and 45 for passes 3/4. Save both, scan only final pass 4. A subsequent source-grounded cleanup must be scored separately; never claim its predecessor's score. This is adaptive exploratory work, not the initial frozen three-arm result.

HIP4 raw measured 45.9%, but its raw claims have clear semantic drift. Run exactly one source-grounded repair plus surface cleanup with the configured Qwen (thinking off), up to 10 exact-substring edits; retain the same narrow repair contract. Score that exact corrected candidate independently. No more HIP rounds are added in this experiment.
