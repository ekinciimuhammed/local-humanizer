# Round-trip translation probe

2026-09-21, after release ed601d0. Prior turn made concrete progress: 1.5 was deployed and pushed, with measured application output 22.4 on the supplied overview. Faithful automatic zero remains unproven.

This new procedure changes the intermediate representation rather than adding another English style prompt. Freeze three English → Turkish/French/Japanese → English arms, all using the existing connected gemma-4-31B-it at temperature0.9/top-p0.95/max8192, two calls each. No source English is supplied to the return-translation stage. Preserve both intermediate and final outputs, errors, complete settings, and usage. No punctuation correction. Six model calls maximum; at most three initial complete-output ZeroGPT checks. The supplied general AI overview is reused exploratory material; its previous100 score is a dated baseline, not a new scan.

Back-translation is an established paraphrase construction technique, but it also introduces recognizable patterns. This is a testable hypothesis, not a proven detector method: [Nguyen-Son et al.,2019](https://aclanthology.org/W19-8626/) specifically studies detecting translated and back-translated text. [Wieting et al.,2017](https://aclanthology.org/D17-1026/) studies neural translation paraphrase data and its differences from human prose. Neither source establishes current ZeroGPT success.

Score only completed full English outputs; report failures rather than removing them. Separate blind important-meaning review from displayed percentages. A zero requires an exact-text repeat and independent meaning review; a reusable method then requires new text validation. Research scripts are opt-in, outside production and offline tests. Existing settings and credentials remain unchanged.

The new scans use the released local browser helper directly, with its fresh-page, exact-input, timeout and pre-existing-result protections. Save the full submitted text, returned exact SHA256, visible percentage and timestamp; unlike earlier standalone scans this helper deliberately does not save screenshots. The production checker enable/auto settings are not changed.

## Adaptive deterministic structural control
All three round trips passed blinded important-meaning review with minor issues but scored100. Do not integrate this failed method. Test a different hypothesis without any model call: rearrange complete paragraph blocks, preserving every character inside them. Three controls: source100 benefits first [1,0,2,3]; actual app22.4 benefits first [1,0,2,3]; guided repair14.3 responsible-use paragraph first [2,0,1]. All three exact outputs are retained, zero word/punctuation edits, one scan each. Original semantic limitations remain; preserving paragraph strings does not itself prove that discourse references still work. These are exploratory controls, not a proposal to blindly shuffle arbitrary user documents.

## Existing Codex CLI capability probe
Read-only discovery found codex-cli0.154.0 with an existing ChatGPT login. Official noninteractive documentation supports ephemeral exec with saved CLI authentication; no API key creation, reading or transfer is needed. Test one writing-only request using an empty temporary working directory, read-only sandbox, no approvals, shell/apps/plugins/browser/computer/image-generation/multi-agent tools disabled, no web search, and no user/project configuration loaded. Preserve the prompt and final passage, actual reported model and usage, and fail if any tool event is observed. This tests a different available model family; it is not a claim that cloud Codex is a local inference engine or already part of the app. Source remains the supplied general overview. [Official noninteractive mode](https://learn.chatgpt.com/docs/non-interactive-mode), [configuration](https://learn.chatgpt.com/docs/config-file/config-reference).

The first CLI writing call completed with no tool events, but did not report its default model identity. Preserve that uncertainty. Make one explicitly selected gpt-6-astra/high call with the exact same prompt and tool restrictions, following the fetched [official Codex model list](https://learn.chatgpt.com/docs/models). This is a model/effort comparison, not a silent retry. Record requested versus reported identity separately. No new API key is used.

The CLI default general-writing sample scored100. Explicit Astra/high completed with no tool events but did not independently report model identity (requestedModel retained); the recorded reasoning usage is0, so do not infer that hidden reasoning definitely occurred. One final model-family comparison uses the exact source-specific lexical instructions from the previous Gemma0 experiment, now via explicit Astra/high. The earlier Gemma0 had meaning errors; the new output gets its own score/review. No scored control text is supplied as an answer or demonstration.

Explicit Astra general prompt scored100; source-specific lexical instructions scored33. Test one final predeclared CLI arm using the existing unrelated generated appointment-booking style example, with no scored target/control passage in the prompt. Same selected Astra/high and restrictions; one generation and one scan. This tests whether the new model follows the demonstration better than earlier Qwen/Gemma probes.

## Apply measured local preferences
The user explicitly requested quality settings and delegated decisions. The deployed UI still selected Qwen/Original/Balanced/temp0.3 with three guides. Save those non-secret preferences in the private runtime volume, then explicitly select the previously measured Gemma/Strong/Plainspoken/temp0.9/top-p0.95/max8192/120s/streaming profile with review and optional guides off. This does not claim general improvement: previous new-case scores were mixed. Connection URL/key, model enable toggles and imported skills are verified unchanged; no checker consent setting is modified. One new live localhost generation and one public scan verify the actual saved profile. This is a new draw, not a repeat of the earlier22.4 text.

The live app generation scored0 through the website helper, and blind labelF passed important meaning with minor issues. Recheck the EXACT same text in a fresh standalone visible-page session and save a screenshot/hash/time. No post-editing, resampling or invented zero. Previous same-profile output was22.4; do not promise a deterministic zero on every new generation. This is an actual product-path zero candidate, unlike the earlier agent-authored control and meaning-failing standalone Gemma0.
